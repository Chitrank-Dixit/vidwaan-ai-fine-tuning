[training_container]
# Targets the active Docker training container
scripture-training-env ansible_connection=docker ansible_python_interpreter=/app/.venv/bin/python

[local]
# Targets host machine local execution
localhost ansible_connection=local ansible_python_interpreter="{{ ansible_playbook_python }}"
